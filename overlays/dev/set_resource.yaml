apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-resources
  namespace: db
spec:
  hard:
    requests.cpu: 200m
    requests.memory: 512Mi
    limits.cpu: 200m
    limits.memory: 512Gi
    requests.storage: 10Gi
